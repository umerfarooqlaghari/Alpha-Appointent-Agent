import { CheckCircle2, Database, XCircle } from "lucide-react";
import { AdminNav } from "@/components/admin-nav";
import { adminApi } from "@/lib/api";
export const dynamic = "force-dynamic";
export default async function StatusPage() { let healthy = true; let message = "PostgreSQL accepted a connection and responded to a health query."; try { const health = await adminApi<{ database: boolean }>("/health"); healthy = health.database; } catch (error) { healthy = false; message = error instanceof Error ? error.message : "Unable to connect to the backend API."; } return <><AdminNav /><main className="mx-auto max-w-3xl px-6 py-10"><p className="text-sm font-semibold text-teal-700">PLATFORM HEALTH</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">System status</h1><section className="mt-8 rounded-lg border border-slate-200 bg-white p-6"><div className="flex items-center gap-4"><div className={`grid size-12 place-items-center rounded-full ${healthy ? "bg-emerald-100 text-emerald-700" : "bg-rose-100 text-rose-700"}`}>{healthy ? <CheckCircle2 /> : <XCircle />}</div><div><h2 className="font-semibold">{healthy ? "Operational" : "Connection failed"}</h2><p className="mt-1 text-sm text-slate-500">Database status checked through the backend API.</p></div></div><div className="mt-6 rounded-md bg-slate-50 p-4"><div className="flex items-center gap-2 text-sm font-medium"><Database size={16} /> PostgreSQL</div><p className="mt-2 break-words text-sm text-slate-600">{message}</p></div></section></main></>; }